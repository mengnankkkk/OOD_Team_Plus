import { NextRequest,NextResponse } from "next/server";
import { getDatabase,getRequestContext,meta } from "@/server/http/context";
export async function PATCH(req:NextRequest,{params}:{params:Promise<{id:string}>}){const{id}=await params;if(!req.headers.get("If-Match"))return NextResponse.json({error:{code:"INVALID_REQUEST",message:"If-Match required"}},{status:400});const body=await req.json().catch(()=>null) as {action?:"MARK_READ"|"IGNORE"}|null;if(body?.action!=="MARK_READ"&&body?.action!=="IGNORE")return NextResponse.json({error:{code:"INVALID_REQUEST",message:"action must be MARK_READ or IGNORE"}},{status:400});const db=getDatabase();const field=body.action==="MARK_READ"?"read_at":"dismissed_at";const value=new Date().toISOString();const result=db.prepare(`UPDATE notifications SET ${field}=COALESCE(${field},?) WHERE id=? AND user_id=?`).run(value,id,getRequestContext(req).userId);if(!result.changes){db.close();return NextResponse.json({error:{code:"RESOURCE_NOT_FOUND",message:"Notification not found"}},{status:404});}const row=db.prepare("SELECT * FROM notifications WHERE id=?").get(id);db.close();return NextResponse.json({data:row,meta:meta()});}
export async function GET(req:NextRequest,{params}:{params:Promise<{id:string}>}){const{id}=await params;const db=getDatabase();const row=db.prepare("SELECT * FROM notifications WHERE id=? AND user_id=?").get(id,getRequestContext(req).userId);db.close();if(!row)return NextResponse.json({error:{code:"RESOURCE_NOT_FOUND",message:"Notification not found"}},{status:404});return NextResponse.json({data:row,meta:meta()});}
